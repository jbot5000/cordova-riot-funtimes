riot.tag2('app', '<h1>Wee!</h1> <device-ready></device-ready> <turd></turd>', '', 'id="app"', function(opts) {
});

riot.tag2('device-ready', '<p class="event listening blink" hide="{received}">Connecting to Device</p> <p class="event received" show="{received}">Device is Ready</p>', '', 'id="deviceready"', function(opts) {
        var tag = this;
        tag.received = false;
        console.log('The script is loading!');
        tag.deviceReady = {

            initialize: function() {
                document.addEventListener('deviceready', this.onDeviceReady.bind(this), false);
            },

            onDeviceReady: function() {
                this.receivedEvent();
            },

            receivedEvent: function() {
                tag.received = true;
                tag.update();

                console.log('Received Event!');
            }
        };

        tag.deviceReady.initialize();
});

riot.tag2('turd', '<button onclick="{displayTurdness}">Are you a turd?</button>', '', '', function(opts) {
        var tag = this;

        tag.displayTurdness = () => {
            console.log('💩');
        };

});
